module.exports = {
	// Your Next.js configuration options go here
	// For example:
	// webpack: (config, { isServer }) => {
	//   if (isServer) {
	//     // configure server-side rendering behavior
	//   }
	//   return config;
	// },
};
