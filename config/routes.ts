export enum SUB_ROUTES {
	EXPENSES = "/expenses",
	DASHBOARD = "/dashboard",
	BLOG = "/blog",
	HOME = "/",
	LOGIN = "/login",
}

export enum MAIN_ROUTES {
	CALCULATOR = "/calculator",
	EXPENSES = "/expenses",
	DASHBOARD = "/dashboard",
	BLOG = "/blog",
	HOME = "/",
	LOGIN = "/login",
	TEST = "/test",
}
